using UnityEngine;

public class BulletBehavior : MonoBehaviour
{
    // each bullet remembers its index in the bullets array
    // this helps avoid looping through bullets to find this bullet
    public int index;
}
